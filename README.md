# Video Agent — LLM as Director

作为一个懒狗，我总是想尽办法偷懒。
现在代码都是vibecoding了，你还指望我自己手动剪辑吗？
这就是本项目的由来，一个像claudecode/codex那样的剪辑agent

傻瓜式操作：丢进去一个录屏，说一句"剪成30秒抖音"，剩下的事不用管。

```
>>> 把桌面上那个录屏剪成30秒抖音视频，以最终效果作为开头
```

## 它怎么工作的

不是那种"选片段→填参数→点导出"的固定流水线。
更像是雇了个编导——你说意图，它自己决定怎么干。

Agent 会先压缩视频方便迭代，然后用 Gemini 把视频从头看一遍、把语音一句一句转录出来（带毫秒时间戳），再给你出一份编导方案，等你点头之后才开始动刀。

改了主意也没关系，直接说，它会重新出方案再确认，不会偷偷就改了。

效果满意之后，它再拿原始高清素材重跑一遍，输出最终视频。

## 跑起来

先把这几个装好：
- Python 3.11+
- `ffmpeg` / `ffprobe`
- `Noto Sans CJK` 字体（中文贴字用）

```bash
pip install -e .
cp .env.example .env
# 填上 GEMINI_API_KEY
```

然后：

```bash
videoagent              # 在当前目录启动
videoagent /你的项目路径  # 指定工作目录
```

启动后直接自然语言描述就行，`/quit` 退出。

## 环境变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `GEMINI_API_KEY` | Gemini API 密钥 | 必填 |
| `GEMINI_MODEL` | 模型名称 | `gemini-3.1-pro-high` |
| `GEMINI_BASE_URL` | API 代理地址 | — |
| `COMPRESS_SHORT_SIDE` | 压缩短边分辨率 | `720` |
| `COMPRESS_BITRATE` | 压缩码率 | `1M` |
| `WORKSPACE_DIR` | 工作目录 | 输入文件同级 `workspace/` |

## 能干什么

剪切、拼接、裁成竖版、加速、加字幕、贴文字、提音频……常见操作都有专用工具，做不到的用 FFmpeg 命令兜底，基本上没有搞不定的。

字幕支持 `[[高亮词]]` 语法，自动渲染成黄色（颜色可改）。

## 未来计划

当前的项目并没有想象的好用，但是整个项目的核心是基于gemini的多模态特性，相信在llm迭代变强的过程中，遇到的问题会被更强的模型自然而然的解决

## 待实现

一个比在黑色命令行里敲自要好一点的ui

特效

转场效果